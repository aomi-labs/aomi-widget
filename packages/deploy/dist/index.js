// src/client.ts
import { Octokit } from "@octokit/rest";

// src/errors.ts
var DeployError = class extends Error {
  code;
  reason;
  constructor(code, message, reason) {
    super(message);
    this.name = "DeployError";
    this.code = code;
    this.reason = reason;
  }
};
var BrowserEnvironmentError = class extends DeployError {
  constructor(message) {
    super("BROWSER_ENVIRONMENT", message);
    this.name = "BrowserEnvironmentError";
  }
};
var PathScopeError = class extends DeployError {
  path;
  constructor(path, message) {
    super("PATH_SCOPE", message);
    this.name = "PathScopeError";
    this.path = path;
  }
};
var TagWideningError = class extends DeployError {
  requested;
  allowed;
  constructor(requested, allowed) {
    super(
      "TAG_WIDENING",
      `target_tags ${JSON.stringify(requested)} must be a subset of the build's server_tags ${JSON.stringify(allowed)}; re-deploy with the wider tag instead`
    );
    this.name = "TagWideningError";
    this.requested = requested;
    this.allowed = allowed;
  }
};
var ActivationError = class extends DeployError {
  status;
  body;
  constructor(status, message, body) {
    super("ACTIVATION", message);
    this.name = "ActivationError";
    this.status = status;
    this.body = body;
  }
};

// src/deployment-json.ts
import { createHash as createHash2 } from "crypto";

// src/release-tag.ts
import { createHash } from "crypto";
var COMMIT_RE = /^[0-9a-f]{12,40}$/;
var DEFAULT_RELEASE_TAG_CONVENTION = "apps-{app_slug}-{short_commit}";
function shortCommit(commit) {
  return commit.slice(0, 12);
}
function assertValidCommit(commit) {
  if (!COMMIT_RE.test(commit)) {
    throw new DeployError(
      "INVALID_COMMIT",
      `source commit must match ${COMMIT_RE} (12\u201340 lowercase hex), got ${JSON.stringify(commit)}`
    );
  }
}
function releaseTag(slug, commit, convention = DEFAULT_RELEASE_TAG_CONVENTION) {
  assertValidCommit(commit);
  return convention.replaceAll("{app_slug}", slug).replaceAll("{short_commit}", shortCommit(commit));
}
function deriveSourceCommit(files) {
  const lines = files.map((f) => `${f.path}\0${f.sha256}\0${f.bytes}`).sort();
  return createHash("sha256").update(lines.join("\n")).digest("hex").slice(0, 40);
}

// src/deployment-json.ts
var RESERVED_MANIFEST_PATH = ".aomi/deployment.json";
var DEPLOYMENT_DIR = ".aomi";
var DEPLOYMENT_FILE = "deployment.json";
function normalizeGithubRepo(repo) {
  let r = repo.trim();
  for (const prefix of [
    "git@github.com:",
    "ssh://git@github.com/",
    "https://github.com/",
    "http://github.com/",
    "github.com/"
  ]) {
    if (r.startsWith(prefix)) {
      r = r.slice(prefix.length);
      break;
    }
  }
  if (r.endsWith(".git")) r = r.slice(0, -4);
  return r.replace(/^\/+|\/+$/g, "").toLowerCase();
}
function toBytes(content) {
  return typeof content === "string" ? Buffer.from(content, "utf8") : content;
}
function sha256Prefixed(content) {
  return `sha256:${createHash2("sha256").update(toBytes(content)).digest("hex")}`;
}
function toPosix(path) {
  return path.replace(/\\/g, "/").replace(/^\.\//, "");
}
function stageFiles(slug, bundle) {
  const entries = Object.entries(bundle);
  if (entries.length === 0) {
    throw new DeployError("EMPTY_BUNDLE", `no files to deploy for app \`${slug}\``);
  }
  const seen = /* @__PURE__ */ new Set();
  const staged = [];
  for (const [rawPath, content] of entries) {
    const path = toPosix(rawPath);
    if (path === "" || path === ".") {
      throw new PathScopeError(rawPath, `empty staged file path`);
    }
    if (path.startsWith("/")) {
      throw new PathScopeError(rawPath, `staged file path must be relative, got ${JSON.stringify(rawPath)}`);
    }
    if (path.split("/").includes("..")) {
      throw new PathScopeError(rawPath, `staged file path may not contain '..': ${JSON.stringify(rawPath)}`);
    }
    if (path === RESERVED_MANIFEST_PATH) {
      throw new PathScopeError(
        rawPath,
        `'${RESERVED_MANIFEST_PATH}' is generated by the deploy client and may not be supplied`
      );
    }
    if (seen.has(path)) {
      throw new PathScopeError(rawPath, `duplicate staged file path: ${path}`);
    }
    seen.add(path);
    const bytes = toBytes(content);
    staged.push({
      path,
      sha256: sha256Prefixed(bytes),
      bytes: bytes.length
    });
  }
  staged.sort((a, b) => a.path < b.path ? -1 : a.path > b.path ? 1 : 0);
  return staged;
}
function buildDeploymentManifest(args) {
  const { slug, descriptor, staged, sourceCommit, serverTags, isPublic } = args;
  const appPath = `${descriptor.app_path_prefix.replace(/^\/+|\/+$/g, "")}/${slug}`;
  const gitUrl = `https://github.com/${descriptor.source_repo}`;
  const now = args.now ?? Math.floor(Date.now() / 1e3);
  return {
    app: {
      name: slug,
      display_name: args.displayName ?? slug,
      platform: descriptor.name,
      git: gitUrl,
      public: isPublic,
      server_tags: serverTags
    },
    source: {
      commit: sourceCommit
    },
    platform: {
      name: descriptor.name,
      github_repo: descriptor.source_repo,
      resolved_deploy_branch: descriptor.publish_branch
    },
    target: {
      branch: descriptor.publish_branch,
      app_path: appPath,
      release_tag: releaseTag(slug, sourceCommit, descriptor.release_tag_convention),
      server_tags: serverTags
    },
    state: {
      pushed: true,
      deployed: true,
      activated: false
    },
    files: staged,
    updated_at: now
  };
}
function validateManifest(manifest, descriptor) {
  const fail = (m) => {
    throw new DeployError("GITHUB_COMMIT", `deployment manifest invalid: ${m}`);
  };
  if (manifest.platform?.name !== descriptor.name) {
    fail(`platform.name must be ${descriptor.name}`);
  }
  if (normalizeGithubRepo(manifest.platform.github_repo) !== normalizeGithubRepo(descriptor.source_repo)) {
    fail(`platform.github_repo must resolve to ${descriptor.source_repo}`);
  }
  const slug = manifest.app?.name;
  if (!slug) fail("app.name must be a non-empty string");
  if (!/^[0-9a-f]{12,40}$/.test(manifest.source?.commit ?? "")) {
    fail("source.commit must be 12\u201340 lowercase hex");
  }
  if (!Array.isArray(manifest.files) || manifest.files.length === 0) {
    fail("files must be a non-empty array");
  }
  const expectedAppPath = `${descriptor.app_path_prefix.replace(/^\/+|\/+$/g, "")}/${slug}`;
  if (manifest.target?.app_path !== expectedAppPath) {
    fail(`target.app_path must be ${expectedAppPath}`);
  }
  const expectedTag = releaseTag(slug, manifest.source.commit, descriptor.release_tag_convention);
  if (manifest.target.release_tag !== expectedTag) {
    fail(`target.release_tag must be ${expectedTag}, got ${manifest.target.release_tag}`);
  }
}

// src/github.ts
var BLOB_MODE = "100644";
function splitRepo(repo) {
  const [owner, name] = repo.split("/");
  if (!owner || !name) {
    throw new DeployError("GITHUB_COMMIT", `github repo must be "owner/repo", got ${JSON.stringify(repo)}`);
  }
  return { owner, repo: name };
}
var GitHubCommitter = class {
  api;
  owner;
  repo;
  branch;
  constructor(api, repo, branch) {
    this.api = api;
    const parts = splitRepo(repo);
    this.owner = parts.owner;
    this.repo = parts.repo;
    this.branch = branch;
  }
  /**
   * Commit the app source + generated manifest under `apps/<slug>/` to the
   * publish branch in a single commit. Returns the new commit SHA.
   */
  async commitApp(args) {
    const { owner, repo, branch } = this;
    const appPath = args.manifest.target.app_path;
    try {
      const ref = await this.api.git.getRef({ owner, repo, ref: `heads/${branch}` });
      const baseCommitSha = ref.data.object.sha;
      const baseCommit = await this.api.git.getCommit({ owner, repo, commit_sha: baseCommitSha });
      const baseTreeSha = baseCommit.data.tree.sha;
      const tree = [];
      for (const file of args.staged) {
        const content = args.files[file.path] ?? findOriginalKey(args.files, file.path);
        if (content === void 0) {
          throw new DeployError("GITHUB_COMMIT", `staged file missing from bundle: ${file.path}`);
        }
        const blob = await this.api.git.createBlob({
          owner,
          repo,
          content: Buffer.from(toBytes(content)).toString("base64"),
          encoding: "base64"
        });
        tree.push({ path: `${appPath}/${file.path}`, mode: BLOB_MODE, type: "blob", sha: blob.data.sha });
      }
      const manifestJson = JSON.stringify(args.manifest, null, 2) + "\n";
      const manifestBlob = await this.api.git.createBlob({
        owner,
        repo,
        content: Buffer.from(manifestJson, "utf8").toString("base64"),
        encoding: "base64"
      });
      tree.push({
        path: `${appPath}/${DEPLOYMENT_DIR}/${DEPLOYMENT_FILE}`,
        mode: BLOB_MODE,
        type: "blob",
        sha: manifestBlob.data.sha
      });
      const newTree = await this.api.git.createTree({ owner, repo, base_tree: baseTreeSha, tree });
      const commit = await this.api.git.createCommit({
        owner,
        repo,
        message: args.message,
        tree: newTree.data.sha,
        parents: [baseCommitSha]
      });
      await this.api.git.updateRef({ owner, repo, ref: `heads/${branch}`, sha: commit.data.sha });
      return commit.data.sha;
    } catch (err) {
      if (err instanceof DeployError) throw err;
      throw new DeployError("GITHUB_COMMIT", `failed to commit app \`${args.slug}\` to ${branch}: ${errMsg(err)}`, err);
    }
  }
  /** CI + release readiness for a slug's latest matching release tag. */
  async status(slug, tagPrefix) {
    const { owner, repo, branch } = this;
    let release = "absent";
    let releaseTag2 = null;
    try {
      const releases = await this.api.repos.listReleases({ owner, repo, per_page: 100 });
      const match = releases.data.filter((r) => !r.draft && r.tag_name.startsWith(`${tagPrefix}-${slug}-`)).at(0);
      if (match) {
        releaseTag2 = match.tag_name;
        release = match.assets.length > 0 ? "ready" : "building";
      }
    } catch (err) {
      throw new DeployError("GITHUB_COMMIT", `failed to read releases for \`${slug}\`: ${errMsg(err)}`, err);
    }
    let ci = "unknown";
    try {
      const runs = await this.api.actions.listWorkflowRunsForRepo({ owner, repo, branch, per_page: 1 });
      const run = runs.data.workflow_runs.at(0);
      if (run) ci = mapCiStatus(run.status, run.conclusion);
    } catch {
      ci = "unknown";
    }
    return { ci, release, releaseTag: releaseTag2 };
  }
};
function findOriginalKey(files, posixPath) {
  for (const [k, v] of Object.entries(files)) {
    if (k.replace(/\\/g, "/").replace(/^\.\//, "") === posixPath) return v;
  }
  return void 0;
}
function mapCiStatus(status, conclusion) {
  if (status === "completed") {
    return conclusion === "success" ? "success" : "failure";
  }
  if (status === "queued" || status === "pending" || status === "waiting") return "pending";
  if (status === "in_progress") return "running";
  return "unknown";
}
function errMsg(err) {
  return err instanceof Error ? err.message : String(err);
}

// src/activation-request.ts
var ACTIVATION_REQUEST_KIND = "activation_request";
var ACTIVATION_REQUEST_SOURCE = "@aomi-labs/deploy/0.1.0";
var ACTIVATION_REQUEST_EMBED_COLOR = 5793266;
function requireField(value, name) {
  const v = value?.trim();
  if (!v) {
    throw new DeployError("ACTIVATION_REQUEST", `\`${name}\` must not be empty`);
  }
  return v;
}
function buildActivationRequest(input) {
  const email = requireField(input.email, "email");
  if (!email.includes("@")) {
    throw new DeployError("ACTIVATION_REQUEST", `\`email\` must be a valid email address (got ${JSON.stringify(input.email)})`);
  }
  return {
    kind: ACTIVATION_REQUEST_KIND,
    email,
    github_account: requireField(input.githubAccount, "githubAccount"),
    app: requireField(input.app, "app"),
    platform: requireField(input.platform, "platform"),
    repo: requireField(input.repo, "repo"),
    requested_at: input.requestedAt ?? (/* @__PURE__ */ new Date()).toISOString().replace(/\.\d{3}Z$/, "Z"),
    source: ACTIVATION_REQUEST_SOURCE
  };
}
function buildActivationRequestDiscordBody(input, opts) {
  const payload = buildActivationRequest(input);
  const compact = JSON.stringify(payload);
  return {
    content: opts?.opsMention ?? "",
    allowed_mentions: { parse: ["users", "roles"] },
    embeds: [
      {
        title: "Activation request",
        color: ACTIVATION_REQUEST_EMBED_COLOR,
        fields: [
          { name: "Requester", value: payload.email, inline: true },
          { name: "GitHub", value: payload.github_account, inline: true },
          { name: "App", value: payload.app, inline: true },
          { name: "Platform", value: payload.platform, inline: true },
          { name: "Repo", value: payload.repo, inline: false }
        ],
        description: "```json\n" + compact + "\n```"
      }
    ]
  };
}

// src/client.ts
var DEFAULT_BRANCH = "publish";
var DEFAULT_ACTIVATE_PATH = "/api/admin/apps/activate";
var DEFAULT_SERVER_TAGS = ["staging"];
var SLUG_RE = /^[a-z0-9][a-z0-9-]*$/;
var DeploymentClient = class {
  opts;
  descriptor;
  committer;
  branch;
  tagPrefix;
  constructor(opts) {
    assertServerOnly();
    this.opts = opts;
    this.branch = opts.github.branch ?? DEFAULT_BRANCH;
    this.descriptor = opts.descriptor ?? defaultDescriptor(opts.aomi.platform, opts.github.repo, this.branch);
    this.tagPrefix = this.descriptor.release_tag_convention.split("{app_slug}")[0].replace(/-+$/, "");
    const api = resolveGitHubApi(opts);
    this.committer = new GitHubCommitter(api, opts.github.repo, this.branch);
  }
  /**
   * Build a pre-deploy activation request (onboarding ask) for `app`, filling
   * `platform` + `repo` from this client's config. Mirror of `aomi-git request`
   * — same canonical payload, so a request raised here and one from the CLI are
   * indistinguishable to the consumer.
   *
   * If `discord.webhookUrl` is configured, the embed is POSTed and `posted` is
   * true; otherwise nothing is sent and the caller delivers `discordBody`
   * itself. The activation code is NEVER part of this — ops mint + deliver it
   * out-of-band.
   */
  async requestActivation(input) {
    assertValidSlug(input.app);
    const common = {
      email: input.email,
      githubAccount: input.githubAccount,
      app: input.app,
      platform: this.opts.aomi.platform,
      repo: this.descriptor.source_repo,
      requestedAt: input.requestedAt
    };
    const payload = buildActivationRequest(common);
    const discordBody = buildActivationRequestDiscordBody(common, {
      opsMention: this.opts.discord?.opsMention
    });
    let posted = false;
    const webhookUrl = this.opts.discord?.webhookUrl;
    if (webhookUrl) {
      let res;
      try {
        res = await fetch(webhookUrl, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(discordBody)
        });
      } catch (err) {
        throw new DeployError(
          "ACTIVATION_REQUEST",
          `failed to POST Discord webhook: ${err instanceof Error ? err.message : String(err)}`
        );
      }
      if (!res.ok) {
        const text = await res.text().catch(() => "");
        throw new DeployError("ACTIVATION_REQUEST", `Discord webhook returned ${res.status}: ${text.trim()}`);
      }
      posted = true;
    }
    await this.audit({
      action: "request",
      slug: input.app,
      actor: input.actor,
      ts: Date.now()
    });
    return { payload, discordBody, posted };
  }
  /** Commit `apps/<slug>/` to the publish branch. Does NOT activate. */
  async deploy(input) {
    assertValidSlug(input.slug);
    const staged = stageFiles(input.slug, input.files);
    const sourceCommit = input.sourceCommit ?? deriveSourceCommit(staged);
    assertValidCommit(sourceCommit);
    const serverTags = normalizeTags(input.serverTags) ?? DEFAULT_SERVER_TAGS;
    const manifest = buildDeploymentManifest({
      slug: input.slug,
      displayName: input.displayName,
      descriptor: this.descriptor,
      staged,
      sourceCommit,
      serverTags,
      isPublic: input.isPublic ?? false
    });
    validateManifest(manifest, this.descriptor);
    const releaseTag2 = manifest.target.release_tag;
    const publishCommitSha = await this.committer.commitApp({
      slug: input.slug,
      files: input.files,
      staged,
      manifest,
      message: `deploy ${input.slug} (${releaseTag2})`
    });
    await this.audit({
      action: "deploy",
      slug: input.slug,
      releaseTag: releaseTag2,
      targetTags: serverTags,
      actor: input.actor,
      ts: Date.now()
    });
    return {
      releaseTag: releaseTag2,
      publishCommitSha,
      sourceCommit,
      appPath: manifest.target.app_path,
      serverTags,
      ciUrl: `https://github.com/${this.opts.github.repo}/actions`
    };
  }
  /** CI + release readiness for a slug's latest matching release. */
  async getStatus(slug) {
    assertValidSlug(slug);
    return this.committer.status(slug, this.tagPrefix);
  }
  /**
   * Activate a built release on the backend. Call only after `getStatus`
   * reports `release: "ready"`. Enforces narrow-only `target_tags` when the
   * build's `server_tags` are supplied.
   */
  async activate(input) {
    assertValidSlug(input.slug);
    const targetTags = normalizeTags(input.targetTags) ?? (input.targetEnv ? [input.targetEnv] : void 0);
    if (!targetTags || targetTags.length === 0) {
      throw new DeployError("ACTIVATION", "activate requires `targetTags` or `targetEnv`");
    }
    if (input.buildServerTags) {
      assertSubset(targetTags, normalizeTags(input.buildServerTags) ?? []);
    }
    const body = {
      app_slug: input.slug,
      platform: this.opts.aomi.platform,
      source_repo: this.descriptor.source_repo,
      app_release_tag: input.releaseTag,
      source_commit: input.sourceCommit,
      is_public: input.isPublic ?? false,
      target_tags: targetTags,
      github_token: this.opts.github.botPat,
      ...input.displayName ? { display_name: input.displayName } : {}
    };
    const url = this.opts.aomi.backendUrl.replace(/\/+$/, "") + (this.opts.aomi.activatePath ?? DEFAULT_ACTIVATE_PATH);
    let res;
    try {
      res = await fetch(url, {
        method: "POST",
        headers: {
          Authorization: `Bearer ${this.opts.aomi.activationToken}`,
          "Content-Type": "application/json"
        },
        body: JSON.stringify(body)
      });
    } catch (err) {
      throw new ActivationError(0, `activation request failed: ${err instanceof Error ? err.message : String(err)}`);
    }
    const text = await res.text();
    let parsed = text;
    try {
      parsed = text ? JSON.parse(text) : null;
    } catch {
    }
    if (!res.ok) {
      throw new ActivationError(res.status, `activation failed (${res.status})`, text);
    }
    await this.audit({
      action: "activate",
      slug: input.slug,
      releaseTag: input.releaseTag,
      targetTags,
      actor: input.actor,
      ts: Date.now()
    });
    return { activated: true, status: res.status, body: parsed };
  }
  async audit(event) {
    if (this.opts.onAudit) await this.opts.onAudit(event);
  }
};
function assertServerOnly() {
  const g = globalThis;
  if (g.__AOMI_ALLOW_BROWSER__ === true) return;
  if (typeof g.window !== "undefined" && typeof g.document !== "undefined") {
    throw new BrowserEnvironmentError(
      "@aomi-labs/deploy is server-only: it holds the bot PAT and activation token and must never run in a browser. Import it from a server route handler, not client code."
    );
  }
}
function assertValidSlug(slug) {
  if (!SLUG_RE.test(slug)) {
    throw new DeployError(
      "INVALID_SLUG",
      `invalid app slug ${JSON.stringify(slug)}: must match ${SLUG_RE} (lowercase alphanumeric + hyphen)`
    );
  }
}
function normalizeTags(tags) {
  if (!tags) return void 0;
  const cleaned = tags.map((t) => t.trim()).filter((t) => t.length > 0);
  return cleaned.length > 0 ? cleaned : void 0;
}
function assertSubset(requested, allowed) {
  const allowedSet = new Set(allowed);
  const widened = requested.filter((t) => !allowedSet.has(t));
  if (widened.length > 0) {
    throw new TagWideningError(requested, allowed);
  }
}
function defaultDescriptor(platform, repo, branch) {
  return {
    name: platform,
    source_repo: repo,
    publish_branch: branch,
    app_path_prefix: "apps",
    release_tag_convention: "apps-{app_slug}-{short_commit}",
    visibility: "private"
  };
}
function resolveGitHubApi(opts) {
  const injected = opts.octokit;
  if (injected) {
    return injected.rest ?? injected;
  }
  const octokit = new Octokit({ auth: opts.github.botPat });
  return octokit.rest ?? octokit;
}
export {
  ACTIVATION_REQUEST_EMBED_COLOR,
  ACTIVATION_REQUEST_KIND,
  ACTIVATION_REQUEST_SOURCE,
  ActivationError,
  BrowserEnvironmentError,
  COMMIT_RE,
  DEFAULT_RELEASE_TAG_CONVENTION,
  DEPLOYMENT_DIR,
  DEPLOYMENT_FILE,
  DeployError,
  DeploymentClient,
  PathScopeError,
  RESERVED_MANIFEST_PATH,
  TagWideningError,
  assertServerOnly,
  assertValidCommit,
  buildActivationRequest,
  buildActivationRequestDiscordBody,
  buildDeploymentManifest,
  deriveSourceCommit,
  normalizeGithubRepo,
  releaseTag,
  sha256Prefixed,
  shortCommit,
  stageFiles,
  validateManifest
};
//# sourceMappingURL=index.js.map