# 2026-07-21

- The portal now builds its x402 client from `AomiWalletKit` instead of requiring
  a Wagmi wallet client, preserving the active wallet provider's chain switching
  and typed-data signing behavior.
- Completed a browser proof with a real MetaMask provider. Better Auth SIWE
  created the canonical account, the x402 challenge switched MetaMask from Base
  to Base Sepolia, MetaMask approved the typed-data authorization, and the BFF
  retry returned 200. The ledger recorded 16.8266 paid credits and the portal
  rendered `Provider Payment`.
- Removed the temporary local EVM signing harness after the real-wallet proof;
  no test-only x402 signing route remains in the portal.
- Repeated the real MetaMask proof using the repo-owned billing harness. The
  same-origin BFF returned `402`, the signed retry returned `200`, and the
  Coinbase ledger settled 16.9485 credits. Existing messages reloaded because
  the isolated billing database intentionally preserves account history.
