## Client-to-react propagation audit

- traced the upgraded `@aomi-labs/client` control and wallet contract through `@aomi-labs/react` and `@aomi-labs/widget-lib`
- removed duplicated wallet payload normalization from the React headless layer so it now reuses client normalization types/utilities
- added shared `toViemSignTypedDataArgs()` in the client package and switched both the CLI signer and widget `WalletTxHandler` to the same EIP-712 signing preparation path
- hardened app selection fallback so runtime message sends use an authorized app after wallet/auth context changes and added regression coverage for that case
- bumped the published package versions for release: `@aomi-labs/client 0.1.11`, `@aomi-labs/react 0.3.2`, `@aomi-labs/widget-lib 1.2.2`
- rebuilt `@aomi-labs/client`, `@aomi-labs/react`, and `@aomi-labs/widget-lib`
- ran the full test suite successfully: `16` files passed, `134` tests passed, `1` skipped
