# 2026-08-31

## Selective payment-gate auth port

- Ported only the anonymous-account boundary, guest-to-wallet/provider
  transition, strict SIWE/single-prompt behavior, and disposable signer fixes
  into `work/portal-ui-local`.
- Excluded developer-key, payment, credit, and application-gate changes.
- Bumped `@aomi-labs/widget-lib` from 2.0.5 to 2.0.6.
- Verified focused Portal/widget tests, Portal and library TypeScript, targeted
  ESLint, the live guest/sign-out account contract, and anonymous Portal chrome.

## Unified Portal account and wallet UI

- Anonymous sessions now hide the settings gear and use a `Sign in` sidebar
  entry point with an Aomi-styled wallet/auth dialog.
- Authenticated management routes to Settings → Account, which owns account
  naming, connected and linked wallets, sign-in methods, and session actions.
- Connected and linked wallet records are deduplicated by family and address;
  connected-but-unlinked wallets expose an explicit link/sign-in action.
- Signing policies are limited to Para/Privy wallets. Rabby, MetaMask, Phantom,
  and other external wallets remain controlled by their wallet application.
- Bumped `@aomi-labs/widget-lib` from 2.0.6 to 2.0.7 and verified the focused
  tests, both TypeScript checks, ESLint, registry/package build, dry-run pack,
  and live anonymous Portal flow.
