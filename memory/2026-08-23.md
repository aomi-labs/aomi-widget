# 2026-08-23

- Added the authenticated Portal `/v1/pipeline/*` BFF and switched
  `/api/mcp/direct` to the Rust `/v1/pipeline/mcp` presenter by default, with
  `AOMI_PIPELINE_ROLLBACK_MODE=legacy` retaining the TypeScript presenter.
- Generated the checked-in Agent+Pipeline OpenAPI/TypeScript contract from the
  Rust `api-server --print-openapi` artifact. The exact manifest has 23 routes,
  including Pipeline MCP.
- Added `AomiClient.pipeline` discovery, skills, safe tool-call, and run
  methods. The server-owned Gate F contract restricts execution to
  `svm-read-only` with no skills; the client never retries or claims durable
  idempotency and owns no custody/signing behavior.
- Added the `aomi pipeline` CLI namespace and migrated `aomi app list` to the
  shared Pipeline client. Refreshed `@aomi-labs/client` 0.6.3 build artifacts.
- Verification: client publish build and dry-run pack passed; Portal typecheck
  passed; client suite passed (364 tests, 28 skipped); focused BFF/MCP suite
  passed (51 tests); generated OpenAPI check passed.
