# 2026-07-30

## BFF Sentry PR review fixes

- Addressed the independent review of draft PR #425 without changing external
  Sentry, Vercel, database, or dashboard state.
- Made the shared identify/classify/route path defensive end to end: invalid
  response statuses and context use safe fallbacks, routing cannot escape the
  caller's catch block, production gets sanitized console signal when Sentry is
  unavailable, and JWT-shaped scalar values are rejected by the final scrubber.
- Made Account, Better Auth, and proxy observer seams absorb both synchronous
  throws and asynchronous rejections. Build now mutates failure state before
  telemetry and catches the promise returned by run reconstruction.
- Restored established Deploy, Account proxy/token, device/provider auth, MCP,
  GitHub cookie, and Build 404/409/null/response contracts while retaining
  telemetry classification. Network failures deliberately remain Issues per
  the approved design; staging must measure outage volume before production.
- Removed the entire Smither artifact-observer/result-code path. The final PR
  has no Smither source, declaration, version, schema, or database-shape diff
  against its pre-PR revision.
- Verification passed: 53 shared tests and typecheck; 387 package tests; 324
  Portal tests; 386 Build tests; Account, Portal, and Build typechecks; Build
  lint with three existing warnings; both production builds (Portal with
  mock-only build configuration); frozen-lockfile install; and diff checks.
  Full Portal lint remains blocked by twelve documented pre-existing errors.
- Closed the final re-review items: the second live-resume store read now
  reports and degrades to a resumed run, file/download artifact failures carry
  their actual 404/409 fallback status, GitHub callbacks retain their prior
  403-only service-auth redirect, and the Build supervisor retains its prior
  500 body. Added regressions and passed 324 Portal plus 386 Build tests and
  both app type-checks.
