# 2026-08-03

- Fixed Para EVM transaction signing in the main `aomi` worktree. Para authentication had created only a synthetic wallet-registry connection, while the wagmi runtime had no Para connector, so transaction requests failed with `ConnectorNotConnectedError` before Para could request approval.
- The Para provider plugin now owns a Para-aware EVM runtime, installs `@getpara/wagmi-v2-connector` against the authenticated Para client, and promotes an authenticated synthetic Para session to a live wagmi connection.
- Added focused coverage for connector installation and connection gating. Verified the lockfile offline, 10 targeted tests, TypeScript, formatting, package build, and live Portal startup from the main worktree.
- Removed the obsolete `aomi-chat-frontend` worktree plus six clean detached agent worktrees; dirty and named feature worktrees were preserved.
- Fixed the Para sequential EOA fallback reusing a stale nonce for the simulation-fe leg. Wallet execution now waits for each preceding receipt before requesting the next signature and reports confirmed partial prefixes if a later leg fails. Verified through the authenticated Helium Portal: the retry completed both Base transactions, the fee receipt succeeded at nonce 2, and the account nonce advanced from 1 to 3.
- Fixed tool-using assistant turns splitting into two chat bubbles when a hidden persisted system record appeared between the working trace and final text. System transcript records are now excluded before assistant turn identity is formed; live notices still use the notification path and transaction echoes still reconcile tool outcomes. Added the exact tool-system-final regression and verified 47 focused runtime tests, lint, typecheck, package builds, registry generation, and the live local Portal.
