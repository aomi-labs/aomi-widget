- Upgraded all declared `@getpara/*` dependencies in `aomi-widget` to `2.21.0`, covering the landing app, registry package peer dependency, and the AA CLI client package.
- Aligned direct `viem` dependencies in the root workspace and registry package to `^2.47.11` so the Para 2.21 AA stack resolves without the previous `@account-kit/*` peer mismatch.
- Refreshed `pnpm-lock.yaml`, `package-lock.json`, and `packages/client/package-lock.json`, then rebuilt `@aomi-labs/client` successfully to verify the upgraded AA/CLI package still compiles.
- Added AA execution unit coverage for both `7702` batch execution and `4337` single-call execution in the TypeScript CLI client, asserting the adapted Para smart-account path returns the expected tx metadata and delegation behavior.
- Reworked `packages/client/test/aa-e2e-raw.mjs` into a reusable Wallets API live test that can send a real sponsored 2-call batch in `7702`, `4337`, or both modes and print the call ID plus mined tx hash for Alchemy dashboard verification.
- Tightened the live Wallets API test to Ethereum mainnet only and fixed the calls to always be 2 self-transfers with `0` ETH, matching the exact dashboard-validation flow used during manual verification.
- Verified the backend at `http://127.0.0.1:8080` was live, then traced a full AA fee E2E attempt and found the globally installed `aomi` CLI was stale: it still posts `tx_ids` to `/api/simulate`, causing `422` against the updated backend contract.
- Rebuilt the local `@aomi-labs/client` dist and reran the flow with the repo CLI; that removed the `422`, but `/api/simulate` still hangs when relying on session fallback for `from`/`chain_id`, while a direct request with explicit fields returns immediately.
- Confirmed the current backend simulate path still fails on mainnet even with explicit `from`/`chain_id`, returning `batch_success: false` and `eth_sendTransaction returned error: {"code":-32602,"message":"No Signer available"}`, so the CLI fee-injection flow cannot complete end-to-end yet.
- Sent a real raw Wallets API mainnet sponsored batch with 3 calls (`dead`, `dead`, treasury fee) using the product-mono Alchemy key/policy; tx `0x1266a322e5c654030847aea366a187e64a349c7caafbdb32227a2a3c9f14741b` mined in block `24844104` and increased treasury `0x9C7a99480c59955a635123EDa064456393e519f5` by exactly `0.000001 ETH`.
- Added a focused CLI sign-flow regression test in `packages/client/test/cli-wallet-sign.unit.test.ts` that proves `signCommand` passes explicit `{ from, chainId }` into `simulateBatch` and appends the backend-returned treasury fee transfer to the final execution call list.
- Switched the direct-owner Alchemy AA path in `packages/client/src/aa/create.ts` from `@getpara/aa-alchemy` to `@alchemy/wallet-apis`, preserving the existing CLI AA interface while routing live 7702/4337 sends through `wallet_prepareCalls` / `wallet_sendPreparedCalls`.
- Rebuilt the CLI and completed a live mainnet `aomi sign --aa --aa-provider alchemy --aa-mode 7702` run against the real backend using two queued `0 ETH` dead-address calls plus the backend-injected treasury fee; transaction `0xfd0131903c62d8be6b69590ebff5f352f95d3edb525283070769a8c714fdcee7` mined successfully and the trace shows the final call transferred `286226980200 wei` to `0x9C7a99480c59955a635123EDa064456393e519f5`.
