# 2026-09-01

## Wallet auth UI follow-up

- Kept live wallet connections as an explicit input to the unified Settings
  wallet model so linking cannot temporarily collapse a connected wallet into
  a linked-only row.
- Resolved numbered persisted wallet labels to colored MetaMask, Rabby,
  WalletConnect, Coinbase, Phantom, Para, and Privy marks.
- Rebuilt the initial sign-in and single-wallet finish-sign-in states around
  the Settings grouped-row design, with clear Connected, Linked, and Active
  states.
- Made connect and social-auth rows fill the popup width; live measurement on
  the local Portal confirmed 416px rows inside a 418px bordered container.
- Replaced the redundant “Not connected on this device” subtitle with an
  icon-labeled Connect action; live wallets retain the icon-labeled Disconnect
  action.
- Removed the separate Use action. A connected-but-inactive wallet now activates
  by clicking its body; only the active wallet gets the green wash and rail,
  while all connected wallets use a cooler blue status chip.
- Moved selection hover styling to the whole wallet row so it no longer ends at
  the action-column seam, and preserved durable insertion order so selecting a
  wallet never reorders the list.
- Verified 42 focused widget tests, 17 focused Portal tests, scoped ESLint,
  Portal production build, widget package build, and `npm pack --dry-run` for
  `@aomi-labs/widget-lib@2.0.8`.

## Account menu session semantics

- Replaced the raw divider beneath the account/allowance summary with a softly
  grouped header card and more deliberate spacing before menu navigation.
- Replaced the permanently visible ambiguous Disconnect action with one
  collapsed Session & wallet control.
- Expanded choices explain and enforce separate boundaries: Sign out ends the
  Aomi session while keeping wallets connected; Disconnect wallet tears down
  connectors while keeping the Aomi session.
- Added distinct confirmation copy for both actions and bumped the widget
  package to `@aomi-labs/widget-lib@2.0.9`.

## Account menu quick wallet switch

- Added a compact stacked-wallet affordance inside the grouped account summary.
- The inline switcher lists live EVM connectors in stable order, marks the
  active wallet, and activates another wallet without opening Settings.
- Added a small Add wallet path from the same surface into the redesigned
  wallet picker and bumped `@aomi-labs/widget-lib` to `2.0.10`.

## General account identity summary

- General now uses the live Aomi account display name before email fallbacks;
  it no longer presents the active wallet address as the account identity.
- General and Account share one wallet summary that calls out linked wallets
  not connected on the current device while preserving connected/linked totals.
- Added a restrained profile icon to the General summary so the first row reads
  as an account overview rather than another wallet row.

## In-place account name editing

- Replaced the detached right-side name input with an input in the name's own
  position, leaving the wallet summary stable beneath it.
- Added explicit save and cancel controls with Enter/Escape keyboard behavior;
  edit mode no longer changes the visible title to the generic Account name.

## Working trace start-state redesign

- Replaced the empty full-width Working box shown before the first trace event
  with a compact Thinking pill and a restrained live pulse/dot cadence.
- Softened the populated trace surface and divider, added the same live pulse
  to its header, and turned step counts into quiet badges hidden at zero.
- Bumped the widget package to `@aomi-labs/widget-lib@2.0.11`.

## Working trace motion and alignment polish

- Removed the three decorative Thinking dots and replaced the expanding ping
  with one contained, smoothly breathing live indicator.
- Normalized the compact and expanded headers to fixed optical heights with
  centered labels, icons, count badges, and chevrons.
- Added a subtle fade/scale/vertical handoff when the start state becomes a
  populated trace and bumped `@aomi-labs/widget-lib` to `2.0.12`.

## Working trace optical alignment

- Reduced the compact Thinking pill to 32px and aligned its center with the
  adjacent Aomi mark without shifting normal assistant messages.
- Optically lifted the Thinking label by one pixel while keeping the activity
  signal geometrically centered.
- Rounded completed trace durations to whole seconds (`6s`, never `6.0s`) and
  bumped `@aomi-labs/widget-lib` to `2.0.13`.

## Thinking pill trailing space

- Preserved the aligned left edge and added two pixels of trailing padding so
  the Thinking label has balanced breathing room inside the compact pill.
- Bumped `@aomi-labs/widget-lib` to `2.0.14`.
