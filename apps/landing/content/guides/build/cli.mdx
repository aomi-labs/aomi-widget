---
title: Build CLI
description: Install and use the Aomi CLI while building or validating an integration.
---

# Build CLI

Use the Aomi CLI when you need to validate prompts, inspect sessions, simulate execution, or verify that an app configuration works end to end.

## Install

```bash
npm install -g @aomi-labs/client
```

## Verify

```bash
aomi --version
aomi --prompt "what is the price of ETH?"
```

For the full command surface, read [CLI Reference](/docs/reference/cli).
