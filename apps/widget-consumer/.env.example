# Portal/BFF origin; never point the authenticated widget at the raw backend.
VITE_AOMI_API_URL=http://localhost:3000

# Public Para identifier. Leave blank for providerless mode. Portal's
# PARA_JWT_AUDIENCE must match when this is set.
VITE_PARA_API_KEY=
