VITE_AOMI_API_URL=http://localhost:3000
VITE_AOMI_APPLICATION_ID=1
VITE_PARA_ENVIRONMENT=BETA
# Para public project key — required for the "Para embedded" fixture mode
# (?provider=para). Without it the picker shows browser wallets only.
VITE_PARA_API_KEY=
# Privy public app ID — required for the "Privy embedded" fixture mode
# (?provider=privy).
VITE_PRIVY_APP_ID=
