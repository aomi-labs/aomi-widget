"use client";

import type { AomiWalletOption } from "../types";

export function buildSocialLoginOptions(
  methods: readonly AomiWalletOption[],
): readonly AomiWalletOption[] {
  return methods;
}
