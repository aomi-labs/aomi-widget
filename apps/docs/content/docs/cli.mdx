---
title: CLI
description: Install and run the CLI for EVM scripts and local workflows.
---

# CLI

_(TODO: extract out the CLI and depending crates and open-source; CLI + session + app abstraction; no backend or DB crates)_

Install:

```bash
brew install aomi
```

Usage:

```bash
aomi --no-docs --rpc-url http://ethereum.rpc
```
